{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'tmp=\"$(mktemp)\"; trap \"rm -f \\\"$tmp\\\"\" EXIT; timeout \"${RED_SKILLS_HOOK_STDIN_TIMEOUT_S:-5s}\" cat >\"$tmp\" 2>/dev/null || true; timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CODEX_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook SessionStart --runner codex <\"$tmp\" || printf \"{}\"'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'tmp=\"$(mktemp)\"; trap \"rm -f \\\"$tmp\\\"\" EXIT; timeout \"${RED_SKILLS_HOOK_STDIN_TIMEOUT_S:-5s}\" cat >\"$tmp\" 2>/dev/null || true; timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CODEX_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook PostToolUse --runner codex <\"$tmp\" || printf \"{}\"'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'tmp=\"$(mktemp)\"; trap \"rm -f \\\"$tmp\\\"\" EXIT; timeout \"${RED_SKILLS_HOOK_STDIN_TIMEOUT_S:-5s}\" cat >\"$tmp\" 2>/dev/null || true; timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CODEX_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook Stop --runner codex <\"$tmp\" || printf \"{}\"'"
          }
        ]
      }
    ]
  }
}
