interface:
  display_name: "Dashboard"
  short_description: "Shows a RedSkills operational dashboard with issue, Spec, AFK worker, flow, and DORA-proxy metrics from GitHub plus local AFK state. Use when the user invokes /dashboard, asks for RedSkills process metrics, workers running, open Specs/issues, cycle time, or DORA metrics."
policy:
  allow_implicit_invocation: false
