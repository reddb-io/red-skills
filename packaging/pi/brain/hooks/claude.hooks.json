{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c '(timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook SessionStart --runner claude >/dev/null 2>&1 &) ; printf \"{}\"'"
          }
        ]
      }
    ]
  }
}
