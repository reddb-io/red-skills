interface:
  display_name: "Status"
  short_description: "Report the project Brain store's health - whether Brain initialized in this workspace, the resolved connection string, and how many artifacts and connections are stored. Use when the user says \"brain status\", \"is brain set up\", \"check brain\", or wants to confirm Brain is live before capturing or searching."
policy:
  allow_implicit_invocation: false
