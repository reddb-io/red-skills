---
"@reddb-io/dev": patch
---

Resolve the redskilled MCP adapter against the host's project root instead of the installed plugin directory, and require routed CI plus integrated-tree validation before `land_branch` merges.
