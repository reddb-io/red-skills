---
"@reddb-io/dev": patch
---

Draw the redskilled dashboard table without borders, returning the three terminal rows and the vertical rules the box drawing spent, and bringing the TUI into line with the string dashboard the other surfaces already render.
