---
"@reddb-io/red-skills": patch
---

The daemon cutover has a defined migration for live castle state (#2855). ADR 0130 moves the birth of a Worker to the host-scoped daemon, and nothing said what happens to the state a working machine already carries across that boundary — a running supervisor, Workers in flight the daemon never saw, worktrees, workspaces and durable lanes. The transition is now a known move: a one-time, idempotent boot migration in the ADR 0105 shape (`core/castle-cutover-migration.ts` plans purely, `runtime/castle-cutover-migration.ts` executes best-effort), running at the supervisor launch that is the era boundary — never in a Worker's own boot, which must never quiesce its peers.

**A Worker in flight at cutover time is quiesced, not adopted.** The daemon re-attaches by unit name and a Worker born by the classic path has no unit to be named by; placement is decided at birth because moving a live process between resource units does not move the memory it already charged, so adopting one would mean the daemon holding a budget it cannot enforce. Quiescing costs supervision and never work: the branch, the workspace and the claim stay exactly where they are, and the Ticket returns to the queue through the existing crash reconcile path, to which a stopped Worker is indistinguishable from a crashed one. The classic supervisor is stopped first so it cannot answer that death by spawning a replacement through the path being removed. Durable lanes do not move — the daemon keeps no per-Worker durable record, so there is no third copy to relocate — and only a git worktree registration whose workspace is gone is reclaimed; no branch and no workspace is ever deleted.

The migration stamps `.red/state/castle/cutover.toon` (contract `red.castle.cutover.v1`) naming what it stopped, quiesced and pruned, what the host refused, and every artifact left behind with its reason. That stamp is also the run-once gate, which is what makes a second boot a pure no-op and what keeps a Worker born after the cutover from ever being mistaken for one born before it. The gate on the migration itself is explicit rather than inferred — the caller that births through the daemon passes it, or `RED_CASTLE_CUTOVER=1` declares it — so an unrelated project's running daemon can never quiesce a healthy classic fleet. `docs/CUTOVER.md` carries the rules and the rollback path, which is five reversible steps and never "reinstall".
