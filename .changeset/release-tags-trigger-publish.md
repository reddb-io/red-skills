---
"@reddb-io/dev": patch
---

Push Release-standard version tags with the configured triggering credential so tag-driven npm publication starts immediately, while a missing credential fails before the repository can present an unpublished version as released.
