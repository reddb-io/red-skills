---
"@reddb-io/dev": minor
---

Turn `redskilled dashboard` into a live operational TUI with durable 48-hour token and Ticket throughput graphs, top-level rates and resource pressure, resize-aware Worker progress, and collapsed death receipts. TTY sessions now use Tuiuiu's alternate-screen lifecycle with `q`, `r`, and `v` controls, while pipes retain a single plain snapshot.
