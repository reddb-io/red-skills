---
"@reddb-io/dev": patch
---

Stop the redskilled statusline from reporting healthy states as defects: a Worker that finished its issue and exited zero is a completion rather than an unexplained death, only a class with a real sender earns the `†` mark on the head, and a project with no registration is an idle project rather than one needing repair.
