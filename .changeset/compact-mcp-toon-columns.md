---
"@reddb-io/dev": patch
---

Compact redskilled MCP responses that contain primitive or object arrays into lossless TOON array columns instead of repeating every row key. On the representative Validation schedule, the `gpt-4o` tokenizer measures 83 tokens before and 51 after (32 tokens, 38.6% saved).
