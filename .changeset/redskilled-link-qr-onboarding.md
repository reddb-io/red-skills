---
"@reddb-io/redskilled-mobile": patch
---

Add the `redskilled link` Host onboarding command, a supervised Remote link
companion, one-use pairing URI and terminal QR, and in-app Android QR scanning
with manual-code fallback.
