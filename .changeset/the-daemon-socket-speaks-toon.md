---
"@reddb-io/red-skills": patch
---

The daemon socket speaks TOON, with a migration that survives mixed versions (#2947). Every request and response between a client and a resident daemon was framed as one line of JSON — `resident-core` is shared, so that was the wire for `redskilled` and for the `rsp` resident alike, the largest machine-facing surface in the system and the one the TOON mandate had never reached. It survived because the ratchet guards *file* I/O and a socket is not a file: the daemons' disks were already correct.

**The wire is TOON now, and `resident-wire` states the order that made it safe rather than leaving it to be inferred.** One daemon serves checkouts pinned to different bundle versions (ADR 0130 rule 3), so a new client meeting an old daemon and an old client meeting a new daemon are ordinary states during a rollout, not accidents. Three rules cover every ordering of the two upgrades: every reader takes the dialect off the bytes, so a JSON line is still a frame a new daemon understands; a responder answers in the dialect it was addressed in, so an old client gets JSON back from a daemon that writes TOON to everyone else; and a client writes TOON but downgrades for the rest of the process when a peer proves it could not read it. The proof is exact rather than a string match — a daemon that fails at parse holds no request to echo an id from, so its refusal carries a fresh one — and that same fact is what makes the retry safe: a request the peer never parsed is one it never executed.

**Framing is what a multi-line document costs, and it is paid once, in one place.** A JSON frame still ends at its first newline; a TOON frame ends at a blank line, which is unambiguous because the encoder escapes newlines inside strings, so a blank line cannot occur inside a document. Both daemons read and write through the same shared socket server instead of each keeping a copy of the framing, since a copy is where the two would silently disagree about what a frame is. Each direction of the rollout is proven against real unix sockets, and the real daemon and the real resident are each shown answering a pre-migration JSON caller and a TOON one.
