---
"@reddb-io/red-skills": patch
---

`/report-bug` is user-invoked only. Filing a bug is a claim about the world that a maintainer stands behind, so the model may no longer reach for the skill on its own — the same reason `/go` and the other dispatch skills carry the flag.
