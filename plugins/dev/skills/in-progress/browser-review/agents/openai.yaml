interface:
  display_name: "Browser Review"
  short_description: "Open a generated HTML artifact for live human review - the human annotates an exact element and character range, the agent polls that feedback and iterates. A layout-audit gate blocks \"done\" on a broken render. Use when you want surgical element-level annotation instead of \"screenshot + describe in prose\", or need a ground-truth render check before declaring UI work done."
