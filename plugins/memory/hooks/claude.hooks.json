{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook SessionStart --runner claude || printf \"{}\"'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook PostToolUse --runner claude || printf \"{}\"'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook Stop --runner claude || printf \"{}\"'"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'timeout \"${RED_SKILLS_HOOK_TIMEOUT_S:-3s}\" node \"${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap.mjs\" hook PreCompact --runner claude || printf \"{}\"'"
          }
        ]
      }
    ]
  }
}
