interface:
  display_name: "Improve Skills"
  short_description: "Generate approval-gated Skill improvement proposals from Memory Skill telemetry. Use when repeated failures suggest a skill should be patched, but direct self-modification would be unsafe."
policy:
  allow_implicit_invocation: false
