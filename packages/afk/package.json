{
  "name": "@reddb/afk",
  "version": "0.1.0",
  "private": true,
  "description": "RedSkills AFK TypeScript runtime: typed CLI, state-machine modules, runner adapters, and compatibility delegation for the legacy shell orchestrator. Built to a single committed bundle that ships in the plugin cache.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "red-afk": "dist/cli.js"
  },
  "scripts": {
    "dev": "node --import tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "compile": "tsc -p tsconfig.build.json",
    "bundle": "esbuild src/cli.ts --bundle --platform=node --format=esm --target=node22 --outfile=../../plugins/dev/skills/engineering/afk/bin/afk.mjs --banner:js=\"import { createRequire as __cr } from 'node:module'; const require = __cr(import.meta.url);\"",
    "build": "pnpm run bundle",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
